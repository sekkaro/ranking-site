import React from "react";

const Admin = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      This is the admin dashboard
    </div>
  );
};

export default Admin;
